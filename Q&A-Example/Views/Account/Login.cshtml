@{
    ViewData["Title"] = "ورود";
}

<div class="min-h-screen flex items-center justify-center bg-gray-100 py-6">
    <div class="bg-white p-8 rounded-xl shadow-md w-full max-w-md text-right">
        <h2 class="text-2xl font-bold mb-6 text-center text-indigo-600">ورود به حساب</h2>

        <form asp-action="Login" method="post" class="space-y-4">
            <div>
                <label class="block text-sm font-medium text-gray-700 mb-1">نام کاربری</label>
                <input name="username" class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring focus:ring-indigo-100 focus:border-indigo-400" required />
            </div>

            <div>
                <label class="block text-sm font-medium text-gray-700 mb-1">رمز عبور</label>
                <input type="password" name="password" class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring focus:ring-indigo-100 focus:border-indigo-400" required />
            </div>

            <button type="submit" class="w-full bg-indigo-600 text-white py-2 rounded-lg hover:bg-indigo-700 transition">
                ورود
            </button>

            <p class="text-sm text-center mt-4 text-gray-600">
                هنوز ثبت‌نام نکردی؟ <a href="/Account/Register" class="text-indigo-600 hover:underline">ثبت‌نام</a>
            </p>
        </form>

        @if (!ViewData.ModelState.IsValid)
        {
            <div class="mt-4 text-sm text-red-500">
                نام کاربری یا رمز عبور نادرست است.
            </div>
        }
    </div>
</div>